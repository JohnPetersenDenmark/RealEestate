import { EstateFilteringPipe } from './estate-filtering.pipe';

describe('EstateFilteringPipe', () => {
  it('create an instance', () => {
    const pipe = new EstateFilteringPipe();
    expect(pipe).toBeTruthy();
  });
});
